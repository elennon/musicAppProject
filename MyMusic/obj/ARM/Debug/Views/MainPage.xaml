<Page
    x:Class="MyMusic.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyMusic.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource TextStyleLargeFontSize}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    >
    
    <Page.Resources>
        <DataTemplate x:Key="HubSectionHeaderTemplate">
            <TextBlock Margin="0,0,0,-9.5" Text="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Key="TilesTemplate">
            <Grid Margin="0,0,9.5,9.5" Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}">
                <Image Source="{Binding ImagePath}" Opacity="0.6" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}" Height="138.5" Width="160"/>
                <TextBlock Text="{Binding Title}" VerticalAlignment="Bottom" Margin="9.5,0,0,6.5" Style="{ThemeResource BaseTextBlockStyle}"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    
    <Grid x:Name="LayoutRoot"  >
        
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        
        <!--<Grid.Background>
            <ImageBrush Opacity="1" ImageSource="/Assets/672.png"/>
        </Grid.Background>-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <StackPanel Orientation="Horizontal" Margin="10,10,0,18" >
                <Image Width="50" Height="50" Name="imgNote"  Source="/Assets/Picture1.png" Stretch="Fill"></Image>
                <TextBlock Text="MY MUSIC" Style="{StaticResource BaseTextBlockStyle}" Margin="12,0"/>
            </StackPanel>

        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">

            <Hub x:Name="Hub" x:Uid="Hub" >
                
                <HubSection x:Uid="groups"  Width="Auto"
                          >
                    <DataTemplate>
                        <GridView x:ConnectionId='1'
                        Margin="0,9.5,0,0"
                        ItemsSource="{Binding}"
                        AutomationProperties.AutomationId="ItemGridView"
                        AutomationProperties.Name="Items In Group"
                        ItemTemplate="{StaticResource TilesTemplate}"
                        SelectionMode="None"
                        IsItemClickEnabled="True"
                                                      
                        ContinuumNavigationTransitionInfo.ExitElementContainer="True">
                            <GridView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid />
                                </ItemsPanelTemplate>
                            </GridView.ItemsPanel>
                        </GridView>
                    </DataTemplate>
                </HubSection>

                
            </Hub>



            
            
        </Grid>
        
    </Grid>
    <Page.BottomAppBar>
        <CommandBar IsSticky="True" x:Name="appBar">
            <CommandBar.PrimaryCommands>
                <AppBarButton x:ConnectionId='2' Label="Now Playing" Icon="Audio"                            />
                <AppBarButton x:ConnectionId='3' Label="Short Cut" Icon="Directions"                              />
                <AppBarButton x:ConnectionId='4' Label="tester" Icon="AddFriend"                         />
            </CommandBar.PrimaryCommands>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:ConnectionId='5' Label="Fill DataBase" Icon="Admin"                             />
                <AppBarButton x:ConnectionId='6' Label="Update DataBase" Icon="Add"                            />
                
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>


<!--<StackPanel>
    <ListBox Name="lstOptions" RequestedTheme="Dark" Foreground="AliceBlue" Background="Black"  SelectionChanged="lstOptions_SelectionChanged"  FontSize="{StaticResource TextStyleExtraExtraLargeFontSize}"  >
        <ListBoxItem Content="You Tubes" Tag="Stream" Margin="8,0,12,12" />
        <ListBoxItem Content="Collection" Tag="Collection" Margin="8,0,12,12" />
        <ListBoxItem Content="Radio Stream" Tag="Radio" Margin="8,0,12,12" />
    </ListBox>
    <ListBox Name="lstGenColl" Margin="0,87,0,10"></ListBox>
</StackPanel>-->           

