<Page
    x:Class="MyMusic.bp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyMusic"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid x:Name="Output" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,20">
            <StackPanel>
                <Button Content="Show Popup (using Offset)" Click="ShowPopupOffsetClicked" />
            </StackPanel>
            <Popup VerticalOffset="100" HorizontalOffset="250" x:Name="StandardPopup">
                <Border BorderBrush="{StaticResource ApplicationForegroundThemeBrush}" 
                Background="ForestGreen"
                BorderThickness="2" Width="100" Height="50">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Done" FontSize="18" HorizontalAlignment="Center" />
                        <!--<Button Content="Close" Click="ClosePopupClicked" HorizontalAlignment="Center" />-->
                    </StackPanel>
                </Border>
            </Popup>
        </Grid>
    </Grid>
</Page>
